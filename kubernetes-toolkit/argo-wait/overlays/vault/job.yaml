apiVersion: batch/v1
kind: Job
metadata:
  name: kubernetes-toolkit
spec:
  template:
    spec:
      containers:
        - name: kubernetes-toolkit
          args:
            [
              'wait-for',
              'statefulset',
              '--namespace',
              'vault',
              '--label',
              'app.kubernetes.io/name=vault',
            ]
